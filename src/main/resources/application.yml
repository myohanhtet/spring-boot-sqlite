server:
  port: 8888
spring:
  datasource:
    url: jdbc:sqlite:dblist.db
    driver-class-name: org.sqlite.JDBC
    username: springuser
    password: ThePassword
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: none
    generate-ddl: false
    database-platform: com.myohanhtet.dirctonary.config.SQLiteDialect


